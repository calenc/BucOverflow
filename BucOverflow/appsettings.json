{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
    "AllowedHosts": "*",
    "ConnectionStrings": {
      "localDb": "Data Source=database.db", // Connection string for the DbContext that points to the Q&A database
      "userDb": "Data Source=users.db" // points to the Users database
    }
}
